{{!< default}}
{{!--
    Custom page template — About.
    Select this template in Ghost Admin on the page with slug `about`.

    Layout:
      1. Page header (title + custom_excerpt as bio strapline + optional
         feature image as portrait) — reuses the existing `page-header`
         partial, same treatment as the Works page.
      2. Page body content via {{content}} — author writes the bio,
         "How I work" principles, and "What I do" capability blocks
         directly in the Ghost editor.
      3. Speaking section — auto-queried from pages tagged with the
         internal `#content-experience` tag (same source as the old
         Works page section that has been removed).
      4. Endorsements section — auto-queried from pages tagged with
         the internal `#content-testimonial` tag (same source as the
         old Works page section that has been removed).
--}}

<div class="content-area custom-page">
    <main class="site-main flow" id="main">

        {{> "page-header"}}

        {{#post}}
            {{#if html}}
                <section class="wrapper">
                    <div class="single-content gh-content">
                        {{content}}
                    </div>
                </section>
            {{/if}}
        {{/post}}

        {{!-- Fluid section (see assets/css/blog/fluid-section.css) — a reusable
              header + adaptive card grid. Reuse the classes for any section of
              this shape; do not fork the CSS. The header (eyebrow/title/intro)
              is authored here per use; keep eyebrow + title inside
              .fluid-section-heading so they stay stacked in both layouts.

              Layout: default is a header sidebar beside the card grid. For a
              full-width top banner instead (header on top, cards below, intro
              floated right), add data-layout="banner" to .fluid-section-body.
              Banner suits fewer, wider cards; the sidebar suits more cards.

              The cards below are placeholder content; to make them tag-driven
              later, replace the hardcoded <li>s with a loop over the fluid-card
              partial, e.g.:
                {{#get "pages" filter="tag:hash-content-value" order="published_at asc"}}
                  <ul class="fluid-grid" role="list">
                    {{#foreach pages}}{{> "components/fluid-card"}}{{/foreach}}
                  </ul>
                {{/get}}
              The fluid-card partial already supports feature images.

              Order: What I do (concrete craft) precedes Values (the person
              behind it), so the page reads concrete → reflective → proof. --}}

        {{!-- What I do — the disciplines, framed as craft independent of any
              employer. Sidebar layout (default). --}}
        <section class="section-container fluid-section">
            <div class="fluid-section-body wrapper">
                <header class="fluid-section-header flow">
                    <div class="fluid-section-heading flow">
                        <p class="fluid-section-eyebrow">How I help</p>
                        <h2 class="fluid-section-title">What I do</h2>
                    </div>
                    <p class="fluid-section-intro">Accessibility and design systems work, from early strategy through shipped code.</p>
                </header>
                <ul class="fluid-grid" role="list">
                    <li class="fluid-card">
                        <div class="fluid-card-body">
                            <h3 class="fluid-card-title">Accessibility, embedded</h3>
                            <p class="fluid-card-desc">WCAG-fluent and embedded with the teams I work alongside, owning accessibility quality from the design file through to the shipped fix.</p>
                        </div>
                    </li>
                    <li class="fluid-card">
                        <div class="fluid-card-body">
                            <h3 class="fluid-card-title">Design systems</h3>
                            <p class="fluid-card-desc">Audits, component reviews, accessibility hardening, and the bridge between the design library and the code that ships from it.</p>
                        </div>
                    </li>
                    <li class="fluid-card">
                        <div class="fluid-card-body">
                            <h3 class="fluid-card-title">Design engineering</h3>
                            <p class="fluid-card-desc">A designer who knows enough code to be useful. I prototype to validate, and fix bugs straight in production.</p>
                        </div>
                    </li>
                    <li class="fluid-card">
                        <div class="fluid-card-body">
                            <h3 class="fluid-card-title">Breaking down silos</h3>
                            <p class="fluid-card-desc">Getting designers and developers to a shared language so accessibility survives the handoff.</p>
                        </div>
                    </li>
                </ul>
            </div>
        </section>

        {{!-- Values — discipline-agnostic beliefs (not practices). --}}
        <section class="section-container fluid-section">
            <div class="fluid-section-body wrapper">
                <header class="fluid-section-header flow">
                    <div class="fluid-section-heading flow">
                        <p class="fluid-section-eyebrow">What drives the work</p>
                        <h2 class="fluid-section-title">Values</h2>
                    </div>
                    <p class="fluid-section-intro">A few things I believe, whatever I'm working on.</p>
                </header>
                <ul class="fluid-grid" role="list">
                    <li class="fluid-card">
                        <div class="fluid-card-body">
                            <h3 class="fluid-card-title">Leave it better than I found it</h3>
                            <p class="fluid-card-desc">Clean up the messes I pass through, even the ones I didn't make. I'll revive stale, half-broken work nobody owns, because someone should.</p>
                        </div>
                    </li>
                    <li class="fluid-card">
                        <div class="fluid-card-body">
                            <h3 class="fluid-card-title">Constraints sharpen the work</h3>
                            <p class="fluid-card-desc">Accessibility, performance, plain language. The limits I design within are what make the result resilient, not lesser.</p>
                        </div>
                    </li>
                    <li class="fluid-card">
                        <div class="fluid-card-body">
                            <h3 class="fluid-card-title">Nothing good is built alone</h3>
                            <p class="fluid-card-desc">The best work happens when overlapping efforts merge instead of compete, and when the people closest to the problem are in the room.</p>
                        </div>
                    </li>
                    <li class="fluid-card">
                        <div class="fluid-card-body">
                            <h3 class="fluid-card-title">Clarity is a kindness</h3>
                            <p class="fluid-card-desc">Plain language, honest documentation, and legible interfaces respect people's time and attention.</p>
                        </div>
                    </li>
                </ul>
            </div>
        </section>

        {{!-- Speaking — pulled from pages tagged hash-content-experience.
              Title can be overridden per page via the post's custom field
              labelled "Speaking title" if you want a different heading. --}}
        <section class="section-container experiences section-experiences">
            <header class="fluid-section-header flow wrapper">
                <div class="fluid-section-heading flow">
                    <p class="fluid-section-eyebrow">Talks &amp; workshops</p>
                    <h2 class="fluid-section-title">Speaking</h2>
                </div>
            </header>
            {{#get "pages" include="tags" filter="tag:hash-content-experience" order="published_at desc"}}
                <div class="wrapper">
                    {{#foreach pages}}
                        {{> "components/experience" }}
                    {{/foreach}}
                </div>
            {{/get}}
        </section>

        {{!-- Endorsements — pulled from pages tagged hash-content-testimonial.
              Limit 6 matches the previous Works page treatment so the page
              doesn't get bottom-heavy; raise it if you want more on display. --}}
        <section class="section-container testimonials section-testimonials">
            <header class="fluid-section-header flow wrapper">
                <div class="fluid-section-heading flow">
                    <p class="fluid-section-eyebrow">What people say</p>
                    <h2 class="fluid-section-title">Endorsements</h2>
                </div>
            </header>
            {{#get "pages" include="tags" filter="tag:hash-content-testimonial" limit="6" order="published_at desc"}}
                <div class="testimonials-list wrapper">
                    {{#foreach pages}}
                        {{> "components/testimonial" }}
                    {{/foreach}}
                </div>
            {{/get}}
        </section>

    </main>
</div>

{{#contentFor "body_class"}}{{#if @member}} logged-in{{/if}}{{/contentFor}}
