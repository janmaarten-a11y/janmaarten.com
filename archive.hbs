{{!< default}}

<div class="content-area custom-page">
    <main class="site-main flow" id="main">
        
        {{> "page-header" showCount=false}}

        <section class="category-grid-container wrapper" data-width="medium">
            {{> "components/category-grid"}}
        </section>

        <section class="section-container small">
            {{#match @custom.feed_layout "Minimal"}}
                <div class="post-feed gh-feed wrapper" data-width="medium">
                    {{#foreach posts}}
                        {{> "loop" noDate=true}}
                    {{/foreach}}
                </div>
            {{else}}
                <div class="post-feed gh-feed{{#match @custom.feed_layout "!=" "Minimal"}} expanded{{/match}}{{#match @custom.feed_layout "Right thumbnail"}} right-thumbnail{{/match}}{{#match @custom.feed_layout "Text-only"}} text-only{{/match}} wrapper" data-width="medium">
                    {{#foreach posts}}
                        {{> "loop" noDate=false}}
                    {{/foreach}}
                </div>
            {{/match}}
        </section>

    </main>
</div>

{{#contentFor "body_class"}}{{#if next}} paged-next{{/if}}{{#if @site.logo}}{{#if @site.cover_image}} with-alt-header{{/if}}{{/if}}{{/contentFor}}